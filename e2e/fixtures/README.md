# e2e fixtures

Real test media bundled into the e2e suite so that the tools are exercised
against authentic file shapes — not just synthesized happy paths.

Total committed size: ~400 KB. All fixtures are reproducible via:

```
node scripts/build-fixtures.mjs            # everything
node scripts/build-fixtures.mjs av         # one category: av | image | pdf | doc | archive | data | code
```

The script downloads a single seed (Big Buck Bunny, 1 MB, 10s, CC-BY 3.0,
cached in `node_modules/.cache/fixture-seed/`) and derives the rest. Only
`av/*` requires ffmpeg + network; every other category is pure-Node and
runs offline.

## Provenance + licenses

| Path | License | Source / how it's made |
|---|---|---|
| `av/clip.mp4`, `av/clip.webm`, `av/segment-*.mp4` | CC-BY 3.0 (video frames) + generated audio | Video frames derived from Blender Foundation's *Big Buck Bunny* (https://peach.blender.org/, CC-BY 3.0). Trimmed to 2s @ 320x180 via ffmpeg. Audio is a synthetic sine tone generated by ffmpeg's `lavfi sine=` source (no copyright). |
| `av/audio.mp3`, `av/audio.wav` | Public domain (synthetic) | Pure sine wave from ffmpeg `lavfi sine=`. |
| `image/photo-*.jpg` | MIT (this repo) | Gradient generated by `pngjs`, encoded to JPEG by ffmpeg. EXIF tags injected via `piexifjs`. |
| `image/alpha.png`, `image/gradient.png`, `image/solid-red.png` | MIT (this repo) | `pngjs`-generated. |
| `image/logo.svg` | MIT (this repo) | Hand-authored. |
| `image/animated.gif` | MIT (this repo) | 4-frame loop generated by ffmpeg from PNGs. |
| `image/sample.webp` | MIT (this repo) | ffmpeg-encoded from a PNG gradient. |
| `pdf/*.pdf` | MIT (this repo) | `pdf-lib` with embedded PNG gradient (no third-party content). |
| `document/sample.docx` | MIT (this repo) | Minimal but valid OOXML zip authored by `scripts/build-fixtures.mjs` (verified to parse with mammoth). |
| `document/sample.{md,html}` | MIT (this repo) | Hand-authored. |
| `archive/*.{zip,tar,tar.gz,gz}` | MIT (this repo) | `fflate` + a small custom ustar TAR writer. Contents are MIT plaintext. |
| `data/people.{json,yaml,xml,toml,csv,tsv}` | Public-domain biographical facts; format encoding MIT | Three historical figures (Ada Lovelace, Grace Hopper, Hedy Lamarr) serialised six ways. |
| `code/messy.{js,ts,css,html,json,sql}` | MIT (this repo) | Hand-authored ugly/minified source for beautify + minify exercises. |

## Why this exists

Synthetic fixtures pass when the synthesizer and the parser share assumptions.
Real fixtures catch the mismatches: real PDFs with font tables that pdf.js has
to walk, real EXIF tags with the orientation bit set, real MP4 atoms with
B-frames, etc.

For things we can't realistically generate offline (a Word doc with the
exact OOXML shape Word emits), the build script emits *minimal but real*
artefacts that are still richer than the inline test PDFs that used to live
in the spec files.

## Not included

* **RAR** — not synthesizable without a RAR encoder, and the only freely
  redistributable test RARs ship as GPL test data. The Archive `Extract` tool
  is exercised against the ZIP/TAR/TAR.GZ/GZIP fixtures instead; RAR extraction
  is unit-tested via the upstream `node-unrar-js` package and asserted at the
  hub level with a smoke test of the file picker.
* **HEIC, AVIF, TIFF, BMP** — image hub does accept these but ffmpeg's
  default Windows build doesn't ship encoders for all of them. If we need
  format-specific coverage later, the candidate inputs are `libheif`'s test
  set (LGPL) and Mozilla's `mozjpeg` test images (BSD-3).
